Old grid

<Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. sources choice panel -->
        <GroupBox Name="SourcesChoicePanelGroup" Grid.Row="0" Header="Folders for Backup Copying" Margin="0,0,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Button 
                            Content="{common:Localize Btn_AddFolder}"
                            Width="120"
                            Height="30"
                            Command="{Binding AddFolderCommand}"/>
                    <Button 
                            Content="{common:Localize Btn_RemoveFolder}"
                            Width="140"
                            Height="30"
                            Margin="10,0,0,0"
                            Command="{Binding RemoveFolderCommand}"/>
                    <Button Name="engButton" Content="EN" Command="{Binding SwitchToEnglishCommand}" Width="40" Margin="2"/>
                    <Button Name="ruButton" Content="RU" Command="{Binding SwitchToRussianCommand}" Width="40" Margin="2"/>
                </StackPanel>

                <ListBox x:Name="SourceFoldersListBox"
                         ItemsSource="{Binding SourceFolders}"
                         SelectedItem="{Binding SelectedFolder, Mode=TwoWay}"
                         Height="150"
                         Margin="0,10,0,0"
                         Background="#F5F5F5"
                         SelectionMode="Single">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </GroupBox>

        <!-- 2. target disk choice panel-->
        <GroupBox Name="DiskChoiceGroup" Grid.Row="1" Header="Target disk for backup" Margin="0,0,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Button Content="{common:Localize Btn_ChooseDisk}"
                            Width="120"
                            Height="30"
                            Command="{Binding SelectTargetCommand}"/>
                    
                    <TextBlock Text="{Binding TargetPathText}" 
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               FontWeight="Bold"/>
                </StackPanel>

                <TextBlock Text="{common:Localize Txt_BackupHere}"
                           Margin="0,10,0,0"
                           Foreground="Gray"/>
            </StackPanel>
        </GroupBox>

        <!-- 3. Control panel -->
        <GroupBox Name="AutoBackupPanelGroup" Grid.Row="1" Header="Automatic backup" Margin="0,150,0,0">
            <StackPanel>
                <!-- 1. Enable/configure auto backup -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <CheckBox Content="{common:Localize AutoBack_Check}"
                              VerticalAlignment="Center"
                              IsChecked="{Binding IsAutoBackupEnabled}"/>

                    <Button Content="{common:Localize Btn_ConfigAB}"
                            Width="80"
                            Height="25"
                            Margin="10,0,0,0"
                            Command="{Binding ConfigureAutoBackupCommand}"/>
                </StackPanel>

                <!-- 2. Current disk info -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Current disk:"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"/>

                    <TextBlock Text="{Binding CurrentDriveText}"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="Blue"/>

                    <TextBlock Text="{Binding DrivestatusText}"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{Binding DriveStatusColor}"/>
                </StackPanel>

                <!-- 3. Manual backup button -->
                <Button Content="{common:Localize Btn_ManualBack}"
                        Width="180"
                        Height="35"
                        Margin="0,15,0,5"
                        FontWeight="Bold"
                        Command="{Binding StartBackupCommand}"/>
            </StackPanel>
        </GroupBox>

        <!-- 4. Operation Log -->
        <GroupBox Name="OperationsLogGroup" Grid.Row="1" Header="Operations Log" Margin="0,287,0,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right">
                    
                    <Button x:Name="ClearLogButton"
                            Content="{common:Localize Btn_clearLog}"
                            Width="80"
                            Height="25"
                            Margin="0,0,5,5"                                       
                            Command="{Binding ClearLogCommand}"                        
                            ToolTip="Clear log"/>
                    
                    <Button x:Name="SaveLogButton"
                            Content="{common:Localize Btn_saveLog}"
                            Width="80"
                            Height="25"
                            Margin="0,0,5,5"
                            Command="{Binding SaveLogCommand}"
                            ToolTip="Save log to file"/>
                </StackPanel>

                <ListBox ItemsSource="{Binding LogEntries}"
                         Background="#F5F5F5"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12">

                    <i:Interaction.Behaviors>
                        <common:AutoScrollBehavior/>
                    </i:Interaction.Behaviors>


                    <ListBox.ItemTemplate>
                    <DataTemplate>
                            <TextBlock Text="{Binding}"
                                   TextWrapping="Wrap"
                                   FontFamily="Consolas"
                                   FontSize="12">
                                <TextBlock.Style>
                                       <Style TargetType="TextBlock">
                                           <Style.Triggers>
                                               <DataTrigger Binding="{Binding Type}" Value="Error">
                                                   <Setter Property="Foreground" Value="Red"/>
                                                   <Setter Property="FontWeight" Value="Bold"/>
                                               </DataTrigger>
                                               <DataTrigger Binding="{Binding Type}" Value="Success">
                                                   <Setter Property="Foreground" Value="Green"/>
                                               </DataTrigger>
                                               <DataTrigger Binding="{Binding Type}" Value="Warning">
                                                   <Setter Property="Foreground" Value="Orange"/>
                                               </DataTrigger>
                                               <DataTrigger Binding="{Binding Type}" Value="Progress">
                                                   <Setter Property="Foreground" Value="Blue"/>
                                               </DataTrigger>
                                               <DataTrigger Binding="{Binding Type}" Value="Info">
                                                   <Setter Property="Foreground" Value="Black"/>
                                               </DataTrigger>
                                           </Style.Triggers>
                                       </Style>
                                </TextBlock.Style>
                            </TextBlock>
                    </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="2,1"/>
                            <Setter Property="Focusable" Value="False"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>
        </GroupBox>

        <!-- 5. Status -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem>
                <Button x:Name="ResetSettingsButton"
                        Content="{common:Localize Btn_resetSettings}"
                        Width="100"
                        Height="20"
                        Margin="10,0,0,0"
                        Command="{Binding ResetSettingsCommand}"
                        ToolTip="Reset all settings to default"/>
            </StatusBarItem>
            
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Value="{Binding ProgressValue}" Width="200" Height="20"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
