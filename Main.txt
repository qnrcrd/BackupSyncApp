<Window x:Class="BackupSyncApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BackupSyncApp.Views"
        xmlns:common="clr-namespace:BackupSyncApp.Common"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="{common:Localize AppTitle}" Height="800" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">

    <Window.Resources>
        <!-- Конвертер для цвета текста статуса -->
        <local:StatusColorConverter x:Key="StatusColorConverter"/>
        
        <!-- Конвертер для выделения активной кнопки навигации -->
        <local:ModeToSelectedConverter x:Key="ModeToSelectedConverter"/>

        <!-- Стили для современных кнопок -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0D47A1"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDBDBD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style for nav buttons -->
        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#757575"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0,0,3,0"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1976D2"/>
                </Trigger>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Selected">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#2196F3"/>
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- GroupBoxes style -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    Background="#2196F3"
                                    Padding="15,8"
                                    CornerRadius="4,4,0,0">
                                <TextBlock Text="{TemplateBinding Header}"
                                         Foreground="White"
                                         FontWeight="SemiBold"
                                         FontSize="14"
                                         TextTrimming="CharacterEllipsis"/>
                            </Border>
                            <Border Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,0,1,1"
                                    CornerRadius="0,0,4,4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- left nav panel -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <Grid Margin="0,20,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- nav header -->
                <TextBlock Grid.Row="0"
                           Text="{common:Localize Nav_header}"
                           Foreground="#9E9E9E"
                           FontSize="12"
                           FontWeight="Bold"
                           Margin="15,0,0,10"/>
                
                <!-- nav buttons -->
                <StackPanel Grid.Row="1" Margin="0">
                    <Button Content="{common:Localize Nav_manualBack}"
                            Style="{StaticResource NavButtonStyle}"
                                Tag="{Binding SelectedMode, Converter={StaticResource ModeToSelectedConverter}, ConverterParameter=Manual}"
                            Command="{Binding SwitchToManualModeCommand}"/>

                    <Button Content="{common:Localize Nav_autoBack}"
                            Style="{StaticResource NavButtonStyle}"
                            Tag="{Binding SelectedMode, Converter={StaticResource ModeToSelectedConverter}, ConverterParameter=Auto}"
                            Command="{Binding SwitchToAutoModeCommand}"/>

                    <Button Content="{common:Localize Nav_Log}"
                            Style="{StaticResource NavButtonStyle}"
                            Tag="{Binding SelectedMode, Converter={StaticResource ModeToSelectedConverter}, ConverterParameter=Log}"
                            Command="{Binding SwitchToLogModeCommand}"/>
                </StackPanel>
                
                <!-- divider -->
                <Rectangle Grid.Row="2"
                           Height="1"
                           Fill="#E0E0E0"
                           Margin="15,20,15,10"/>


                <!-- Disk info (always visible) -->
                <StackPanel Grid.Row="3" Margin="15,0,15,20" VerticalAlignment="Bottom">
                    <TextBlock Text="{common:Localize Txt_Current_dsk}"
                               Foreground="#9E9E9E"
                               FontSize="11"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <Border Background="#F5F5F5"
                            CornerRadius="4"
                            Padding="10">
                        <StackPanel>
                            <TextBlock Text="{Binding CurrentDriveText}"
                                       FontWeight="Bold"
                                       Foreground="#2196F3"
                                       FontSize="13"/>
                            <TextBlock Text="{Binding DriveStatusText}"
                                       Foreground="{Binding DriveStatusColor}"
                                       FontSize="12"
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Right content panel -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Source folders | ALWAYS VISIBLE -->
                    <RowDefinition Height="Auto"/> <!-- Content depending on active mode -->
                    <RowDefinition Height="Auto"/> <!-- Status Bar -->
                </Grid.RowDefinitions>
                
                <!-- 1. Source Folders Panel | ALWAYS VISIBLE -->
                <GroupBox Grid.Row="0" Header="{common:Localize Header_SourceFolderPanel}" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button Content="{common:Localize Btn_AddFolder}"
                                    Width="120"
                                    Style="{StaticResource ModernButton}"
                                    Command="{Binding AddFolderCommand}"/>

                            <Button Content="{common:Localize Btn_RemoveFolder}"
                                    Width="130"
                                    Margin="10,0,0,0"
                                    Style="{StaticResource ModernButton}"
                                    Background="#F9F9F9"
                                    Command="{Binding RemoveFolderCommand}"/>

                            <Border Margin="15,0,0,0" Background="#E0E0E0" Width="1" Height="30"/>

                            <StackPanel Orientation="Horizontal" Margin="15,0,0,0">
                                <Button Content="EN"
                                        Width="45"
                                        Margin="2"
                                        Style="{StaticResource ModernButton}"
                                        Background="#607D8B"
                                        Command="{Binding SwitchToEnglishCommand}"/>

                                <Button Content="RU"
                                        Margin="2"
                                        Style="{StaticResource ModernButton}"
                                        Background="#607D8B"
                                        Command="{Binding SwitchToRussianCommand}"/>
                            </StackPanel>
                        </StackPanel>

                        <ListBox x:Name="SourceFoldersListBox"
                                 ItemsSource="{Binding SourceFolders}"
                                 SelectedItem="{Binding SelectedFolder, Mode=TwoWay}"
                                 Height="120"
                                 BorderBrush="#E0E0E0"
                                 Background="White">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               TextTrimming="CharacterEllipsis"
                                               Padding="8,5"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </GroupBox>
                
                <!-- 2. content depending on active mode -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <!-- Manual Backup mode -->
                    <StackPanel Visibility="{Binding IsManualModeVisible}">
                        <GroupBox Header="{common:Localize Header_ManualBackup}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <Button Content="{common:Localize Btn_ChooseDisk}"
                                            Width="150"
                                            Style="{StaticResource ModernButton}"
                                            Command="{Binding SelectTargetCommand}"/>

                                    <Border Background="#F5F5F5"
                                            CornerRadius="4"
                                            Padding="15,8"
                                            Margin="10,0,0,0">
                                        <TextBlock Text="{Binding TargetPathText}" 
                                                 VerticalAlignment="Center"
                                                 FontWeight="SemiBold"
                                                 Foreground="#2196F3"/>
                                    </Border>
                                </StackPanel>

                                <TextBlock Text="{common:Localize Txt_BackupHere}"
                                         Foreground="#757575"
                                         FontStyle="Italic"
                                         Margin="0,0,0,15"/>

                                <Button Content="{common:Localize Btn_ManualBack}"
                                        Width="250"
                                        Height="45"
                                        HorizontalAlignment="Left"
                                        Style="{StaticResource ModernButton}"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Command="{Binding StartBackupCommand}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>

                    <!-- Auto Backup mode -->
                    <StackPanel Visibility="{Binding IsAutoModeVisible}">
                        <GroupBox Header="{common:Localize Header_AutoBackup}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                    <CheckBox Content="{common:Localize AutoBack_Check}"
                                              VerticalAlignment="Center"
                                              IsChecked="{Binding IsAutoBackupEnabled}"
                                              FontSize="13"/>

                                    <Button Content="{common:Localize Btn_ConfigAB}"
                                            Width="100"
                                            Height="30"
                                            Margin="15,0,0,0"
                                            Style="{StaticResource ModernButton}"
                                            Background="#9E9E9E"
                                            Command="{Binding ConfigureAutoBackupCommand}"/>
                                </StackPanel>

                                <Border Background="#F8F9FA"
                                        CornerRadius="4"
                                        Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="{common:Localize Header_AutoBackupSettings}"
                                                 FontSize="11"
                                                 Foreground="#9E9E9E"
                                                 FontWeight="Bold"
                                                 Margin="0,0,0,10"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                                     Text="Status:" 
                                                     Foreground="#757575"
                                                     Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" 
                                                     Text="{Binding AutoBackupStatus}"
                                                     Foreground="{Binding AutoBackupStatusColor}"
                                                     FontWeight="SemiBold"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                                     Text="{common:Localize Txt_LastOpsTime}" 
                                                     Foreground="#757575"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" 
                                                     Text="{Binding LastBackupTime}"
                                                     FontWeight="SemiBold"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>

                    <!-- Operations Log mode -->
                    <StackPanel Visibility="{Binding IsLogModeVisible}">
                        <GroupBox Header="{common:Localize Header_Log}">
                            <DockPanel>
                                <StackPanel DockPanel.Dock="Top" 
                                          Orientation="Horizontal" 
                                          HorizontalAlignment="Right"
                                          Margin="0,0,0,10">
                                    <Button x:Name="ClearLogButton"
                                            Content="{common:Localize Btn_clearLog}"
                                            Width="100"
                                            Margin="0,0,5,0"
                                            Style="{StaticResource ModernButton}"
                                            Background="#9E9E9E"
                                            Command="{Binding ClearLogCommand}"
                                            ToolTip="Clear all log entries"/>

                                    <Button x:Name="SaveLogButton"
                                            Content="{common:Localize Btn_saveLog}"
                                            Width="100"
                                            Style="{StaticResource ModernButton}"
                                            Background="#9E9E9E"
                                            Command="{Binding SaveLogCommand}"
                                            ToolTip="Save log to file"/>
                                </StackPanel>

                                <ListBox ItemsSource="{Binding LogEntries}"
                                         Background="#F8F9FA"
                                         BorderThickness="1"
                                         BorderBrush="#E0E0E0"
                                         FontFamily="Consolas"
                                         FontSize="12"
                                         Height="300">

                                    <i:Interaction.Behaviors>
                                        <common:AutoScrollBehavior/>
                                    </i:Interaction.Behaviors>

                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}"
                                                     TextWrapping="Wrap"
                                                     FontFamily="Consolas"
                                                     FontSize="12"
                                                     Padding="8,4">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Type}" Value="Error">
                                                                <Setter Property="Foreground" Value="#F44336"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="Success">
                                                                <Setter Property="Foreground" Value="#4CAF50"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="Warning">
                                                                <Setter Property="Foreground" Value="#FF9800"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="Progress">
                                                                <Setter Property="Foreground" Value="#2196F3"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="Info">
                                                                <Setter Property="Foreground" Value="#212121"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </DockPanel>
                        </GroupBox>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
